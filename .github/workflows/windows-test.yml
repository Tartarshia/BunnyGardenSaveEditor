name: Windows public checks

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Compile native Windows editor and scan public source
        shell: cmd
        run: test_public.cmd
